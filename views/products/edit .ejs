<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>  Add a Product</h1>

    <form action="/products/<%= product._id %>  ?_method=PUT" method="post">

        <label for="name"> Product name</label>
        <input type="text" name="name" id="name" placeholder="type here" value="<%= product.name%> ">

        <label for="price">Price per unit</label>
        <input type="number" name="price" id="price" placeholder="0"  value="<%= product.price%>">
        <label for="Category">choose category</label>

        <select name="category" id="Category"  value="<%= product.category%>">
        
            <option value="fruit" <%=product.category ==='fruit' ? 'selected' :''  %>  >fruit</option>
            <option value="vegs"  <%=product.category ==='vegs' ? 'selected' :''  %>  >vegs</option>
            <option value="dairy"  <%=product.category ==='dairy' ? 'selected' :''  %>  >dairy</option>
        </select>

        <button type="submit">save product</button>
    </form>

    <a href="/products/<%=product._id%>"> Cancel</a>

</body>
</html>